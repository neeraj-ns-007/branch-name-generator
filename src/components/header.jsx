const Header = () => {
    return(
        <div className='head-container'>
            <div className='head-main'>BRANCH NAME GENERATOR</div>
            <div className="head-desc">Generates a git branch name from a given task name.</div>
        </div>
    );
}

export default Header;
